## Markdown 渲染能力测试

这篇文档用于测试更高级的 Markdown 语法在页面上的显示效果。

### 1. 代码高亮 (Javascript)
```javascript
// 这是一个代码块示例
function sayHello(name) {
    const greeting = `Hello, ${name}!`;
    console.log(greeting);
    return true;
}
